# from django.urls import path

# from products.views import ProductView, SortView, CountryListView, ProductDetailView

# urlpatterns = [
#     path('/', SignUpView.as_view()),
#     path('/login', LogInView.as_view())
# ]